{{- if .Values.phpipam.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.phpipam.name }}
  {{- with .Values.phpipam.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.phpipam.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.phpipam.ingress.tls }}
  tls:
    {{- range .Values.phpipam.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.phpipam.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: phpipam-web
                port:
                  number: 80
          {{- end }}
    {{- end }}
{{- end }}